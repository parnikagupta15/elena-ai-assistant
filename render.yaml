services:
  - type: web
    name: elena-ai-assistant
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.web_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0